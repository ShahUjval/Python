import subprocess

#op = subprocess.check_output(['dir'] , shell=True)
#print op
subprocess.call(['date'])

#p1 = subprocess.Popen(['Dir']) , stdout=subprocess.PIPE , stderr = subprocess.PIPE)
#p2 = subprocess.Popen(['tr','a-z','A-Z'], stdout=subprocess.PIPE, stdin=p1.stdout . stderr=subprocess.PIPE)

#for line in p2.communicate()